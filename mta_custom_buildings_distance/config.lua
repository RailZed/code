-- =====================================================================
-- Конфиг для скрипта дальности кастомных зданий
-- =====================================================================
--
-- Здесь указываются model ID, которые ты заменил кастомными моделями.
-- Для каждого можно задать свою дистанцию прорисовки (LOD distance).
--
-- Почему так, а не setFarClipDistance:
--   * setFarClipDistance УВЕЛИЧИВАЕТ КОЛИЧЕСТВО ВСЕЙ отрисовываемой
--     геометрии в кадре — FPS падает на всём, везде.
--   * engineSetModelLODDistance работает ТОЧЕЧНО по конкретной модели:
--     движок просто перестаёт скрывать её на дистанции, но соседние
--     дома/деревья по-прежнему отсекаются. FPS почти не страдает.
--
-- Безопасные значения LOD: 170 (стандарт SA) ... 500.
-- 300 — золотая середина (видно издалека, FPS не падает).
-- 500+ имеет смысл только для крупных объектов (небоскрёбы, мосты).
--
-- ВАЖНО: если ты заменил модель И собрал к ней LOD-модель (LOD<id>),
-- укажи её ID в поле lodModel — это даст идеальную картинку без
-- мерцания, и FPS останется максимальным.
--
Config = {}

-- Глобальные настройки
Config.defaultLODDistance = 300       -- дистанция по умолчанию для всех моделей ниже
Config.applyOnResourceStart = true    -- применить сразу при старте ресурса
Config.applyOnModelLoad     = true    -- переприменять при стриминге модели
Config.debug                = false   -- печатать отладку в чат/консоль

-- Список твоих кастомных моделей.
-- Формат:
--   { model = <id>, distance = <число> (опционально), lodModel = <id> (опционально) }
--
-- Пример:
--   { model = 1337 },                               -- возьмёт defaultLODDistance
--   { model = 1338, distance = 500 },               -- индивидуально 500
--   { model = 1339, distance = 500, lodModel = 1340 } -- + отдельный LOD объект
--
Config.models = {
    -- TODO: впиши сюда свои model ID
    -- { model = 1337 },
    -- { model = 1338, distance = 400 },
}
